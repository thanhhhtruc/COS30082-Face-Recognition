# Copy this file to `.env.local` in the project root and adjust values.
# The frontend reads API_BASE_URL in next.config.mjs for proxy rewrites.

# Base URL of your backend API (no trailing slash)
API_BASE_URL=http://localhost:8000

